<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.training.MetaGoals">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Slider">
        <activity
            android:name="com.training.MetaGoals.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.training.MetaGoals.ChallengeActivity"/>
        <activity android:name="com.training.MetaGoals.qr_code.QrCodeActicity"/>
        <activity android:name="com.training.MetaGoals.create_challenge.CreateChallenge"/>
        <activity android:name=".qr_code.Capture"/>
    </application>

</manifest>